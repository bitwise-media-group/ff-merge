{
  "name": "ff-merge",
  "version": "1.3.0",
  "description": "GitHub Action that fast-forwards an approved, green pull request into its base branch, preserving the original commit signature",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "rollup --config rollup.config.js",
    "check": "biome check . && npm run lint:md",
    "check:fix": "biome check --write . && npm run lint:md -- --fix",
    "format": "prettier --write \"**/*.md\"",
    "lint:md": "markdownlint-cli2",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "all": "npm run check && npm run typecheck && npm run test && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bitwise-media-group/ff-merge.git"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/github": "9.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@rollup/plugin-commonjs": "29.0.3",
    "@rollup/plugin-json": "6.1.0",
    "@rollup/plugin-node-resolve": "16.0.3",
    "@rollup/plugin-typescript": "12.3.0",
    "@types/node": "26.1.1",
    "@vitest/coverage-v8": "4.1.10",
    "markdownlint-cli2": "0.23.0",
    "prettier": "3.9.5",
    "rollup": "4.62.2",
    "tslib": "2.8.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
